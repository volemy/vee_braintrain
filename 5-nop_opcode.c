#include "monty.h"

/**
 * nop - function that does nothing
 * @stack: pointer to pointerr to stack
 * @line_num: current line number
 *
 */
void nop(stack_t **stack, unsigned int line_num)
{
	UNUSED(stack);
	UNUSED(line_num);

}
